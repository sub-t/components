import { violet } from '@radix-ui/colors';
import { Root } from '@radix-ui/react-label';
import { styled } from '@stitches/react';

export const FormLabel = styled(Root, {
  fontSize: '0.8rem',
  fontWeight: 500,
  color: violet.violet10,
});
